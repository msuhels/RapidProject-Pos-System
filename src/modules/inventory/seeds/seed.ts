/**
 * Inventory Module Seed Data
 *
 * This seed file registers the Inventory module fields.
 * The module itself is auto-discovered by the main seed script.
 */

export default async function seedInventoryModule() {
  // Module fields are registered in the main seed script (scripts/seed.ts)
  // under the INVENTORY key in moduleFieldDefinitions.
  // This file exists for consistency with other modules.
  console.log('ℹ️  Inventory module fields are registered in main seed script');
}

